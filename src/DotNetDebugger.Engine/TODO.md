# TODO（DotNetDebugger.Engine 近期待办）

> 近期待办，完成一项删一项；远期想法见 `docs/ROADMAP.md`；开发指南见同目录 `AGENTS.md`。

> **2026-09-08 清理归档**：原承接宿主主清单的 Engine 侧条目（P2 异常现场/`$exception`/类型过滤、P9 launch 早期接管等）已随宿主 P1-P9 全部完成（细节见 git log 与 `docs/planning/specs/`）。Engine 侧新能力候选（现场改写 SetValue、数据断点等）见 `src/DotNetDebuggerMcp/TODO.md`「agent 自动化调试闭环缺口清单」。当前无独立近期待办。
