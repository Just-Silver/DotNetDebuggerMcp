# TODO（DotNetDebuggerMcp 宿主近期待办）

> 近期待办，完成一项删一项；远期想法见 `docs/ROADMAP.md`；开发指南见同目录 `AGENTS.md`。

## agent 动态调试体验升级（2026-09-06 立项，按优先级串行推进）

> 背景：项目定位是让 agent 用真动态调试替代「加日志-重跑」循环。**每项先计划后执行**：中大型项（P3/P6/P7）先在 `docs/planning/specs/` 立 spec 再动码；小型项先在条目下补 3-5 行方案段（关键取舍）再实现。收尾一律：build + 单测 + Client/CLI 端到端；改 MCP 工具（加参/加工具）须同 commit 改根 README（新参数带默认值、`[Description]` 中文注明默认值）。

- [x] **P1 目标进程 stdout/stderr 转发**（Session+宿主｜小）——**已完成 2026-09-06**：`ProcessOutputCapture` 环形缓冲（500 行）+ `debug_output(lines=50)` 新工具 + `debug_wait` 默认附尾部 20 行；Session 单测/宿主端到端/全量 428 通过。现状排空即丢弃（`DebugSessionManager.cs:84`），agent 看不到目标自己的日志/异常打印/崩溃码。推荐：进程输出环形缓冲（标注流别），`debug_state`/`debug_wait` 返回附尾部 N 行；保留持续排空纪律防管道阻塞。方案取舍点：推进事件流 vs 工具拉取、默认行数、进程退出后是否可取。
  - 方案（2026-09-06）：① 捕获点定在 `LaunchAndAttachAsync`（`debug_launch`/`-dbg` 的真实路径；attach 已运行进程无法重定向其输出，工具提示即可）；② Session 新增 `ProcessOutputCapture` 线程安全环形缓冲（上限 500 行，行带 out/err 流别 + 退出标记行），Process 不再 `using var`、挂到 `ActiveDebugSession.Output`，会话关闭时 Dispose（不杀进程，语义不变）；③ 暴露方式：新工具 `debug_output(lines=50)` 随时拉取（运行中亦可），`debug_wait` 加 `outputLines=20`（默认附尾部，停点/退出/超时三种结果都附）；④ `debug_state` 保持轻量不附输出（高频调用防噪声）——取舍记录在案。
  - ~~遗留：`ProcessOutputLine` 无时间戳~~（**已完成 2026-09-06**：record 加 `Timestamp`，`Append` 入口锁外取 `DateTimeOffset.Now`，`debug_output`/`debug_wait` 展示 `[HH:mm:ss.fff out/err]` 格式）。~~原问题：agent 无法把断点/异常命中与目标日志对时~~
- [x] **P2 异常现场增强 + 类型精确过滤**（Engine+Session+宿主｜小-中）——**已完成 2026-09-06**：`TypeNameResolver`（SRM 行内解析）+ Message 基类链捕获（`CorDebugType.Base` 走到 System.Exception 的 `_message`，字段值是字符串引用需解引用——踩坑实录）+ `$exception` 伪变量 + 过滤接线（不匹配→Continue+skipped 计数反馈，仅 FIRST_CHANCE 决策防同异常多阶段重复停）。Engine 12/Session 13/宿主 429 全绿。现状与推荐见下；方案要点：① SRM TypeDef/TypeRef 行内 namespace+name（嵌套走 enclosing 链）；② `CurrentException` first-chance 停点有效（ClrDebug 实锤）；③ 不停即 Continue（sharpdbg 同款）；④ 匹配=全名相等或 `.短名` 结尾忽略大小写（DecompileMember「从严匹配+可诊断失败」精神，skipped 反馈防静默空等）。
  - 方案（2026-09-06 定稿，经用户确认）：① `TypeNameResolver`（SRM）：TypeDef/TypeRef 行内自带 namespace+name（嵌套走 enclosing 链，无需跨程序集），按 (模块路径, token) 缓存——本地克隆实锤（sharpdbg `GetCorDebugTypeFriendlyName` 同路线，Engine 现用 SRM 模式一致）；② Message 经 `CorDebugThread.CurrentException`（first-chance 停点有效，ClrDebug 文档实锤）读 `_message` 字段（复用 GetFieldValue 管线）填 payload；③ `$exception` 合成进 `GetVariablesAsync` 首行（类型+Message 保真，children 走现有一级展开、读不出诚实标注）；④ 过滤接线：不匹配 → 不停直接 Continue（sharpdbg `HandleException2` 同款）+ DecompileMember 式不命中反馈——引擎发 skipped 事件、Session 计数、`debug_wait`/`debug_state` 附「期间跳过 N 个不在过滤范围的异常（如 Xxx）」；⑤ 匹配语义（已确认）：空=全部；否则全名相等或 `.短名` 结尾、忽略大小写，去掉任意 Contains；⑥ `debug_exceptions` 文案同步生效语义。
- [x] **P3 行断点：反编译视图行 + PDB 源行**（Decompiler/Session+宿主｜中）——**已完成 2026-09-06**（spec `docs/planning/specs/2026-09-06-p3-line-breakpoints.md`，状态已回写）。步骤0 探针实锤：`DecompileAsString`（decompile 工具旧渲染）与 DocumentService 映射渲染**不同系**（+using 头、空行策略不同）→ 已执行 fallback：类型级 decompile 统一走 DocumentService 渲染（顺带修复 agent 行号与 Web 视图历史错位）；`GetBreakpointTargetAtLine`（含 Exact 标志）等四映射助手自 Web 提升为公共实现；新增 `SourceLineResolver`（PDB 序列点；双 reader 坑：portable PDB 无 MethodDef 表，方法枚举必须走 DLL）；`debug_breakpoint_set` 三分支（token / typeName+line / sourcePath+line，行方式跨模块解析+消歧、Exact 落点注明）。全量回归：Decompiler 18/Web 18/Engine 12/Session 13/宿主 430/Client 全通过。
- [x] **P4 停点现场源码上下文**（宿主+Session｜小）——**已完成 2026-09-06**（方案经用户确认：整函数+预算 100+0 关闭，弃 ±5 固定窗）。`debug_wait`/`debug_state` 加 `contextLines` 参数：停点附反编译视图当前语句周边（行号=decompile 输出行号，与 P3 行断点闭环——可直接拿上下文行号设断点）；方法 ≤ 预算显示整个函数、超出按当前语句截取并注明；当前语句标 `← 当前语句`。常量归位：`AppConfig.DefaultStopContextBudgetLines=100`（行为单源）+ `ToolParameterText.ContextLinesParam`（文案；const int 不能参与 const string 拼接——语言限制，文案数字为受注释约束的副本，TimeoutParam 同款先例）。`FindTypeByToken` 自 Web 提升为 `DocumentService` 公共实现；宿主新增 `DebugDocumentCache`（32 项，超限清空）。Decompiler 21/宿主 430/Web 18 全绿。
- [x] **P5 断点命中计数 + trace/log 模式**（Session+宿主｜小-中）——**已完成 2026-09-06**（两个决策点经用户确认：hitCount≥N、轨迹消费式）。实现比原方案更优：trace 不在 Session 层 wait 循环 hit-test，而是**引擎命令泵内**完成（命中即快照→发 TraceHit→不停落默认 Continue，与 skipped 异常同款，进程仅瞬间停顿对目标不可见）；`GetVariables` 同步体提取为 `ReadVariablesForThread` 供 trace 快照复用。Engine 14/Session 14/宿主 431/Web 18/Client 全绿。
  - 方案（2026-09-06，待确认）：① **hitCount**：`DebugBreakpoint` 加 目标次数（默认 1）+ 计数；引擎 `HandleBreakpoint` 计数 <目标 → 不停落默认 Continue，≥目标 → 停（语义=「第 N 次起每次都停」，VS 风格）；pending 重绑计数保留；list 显示 目标/已命中。② **trace**：`debug_breakpoint_set` 加 `mode="stop"|"trace"`（默认 stop）——trace 命中时引擎命令泵内同步读变量快照（GetVariables 内部逻辑提取复用）→ 发 `TraceHit` 事件 → 不停落默认 Continue（与 skipped 异常同款模式）；Session 缓冲折叠进环形轨迹（上限 100 条）+ 消费式读取；`debug_wait`/`debug_state` 附「trace 轨迹（N 条）」段。③ **组合正交**：trace+hitCount（第 N 次起开始记录）。④ 快照深度：栈顶帧 locals+arguments 一级展开（与 debug_variables 同款单行渲染）；更深路径等 P6。⑤ 待拍板：hitCount 语义（建议 ≥N）；轨迹消费式 vs 全量累积（建议消费式+list 显示未读计数）。
- [x] **P6 表达式读值安全子集**（Session 内新组件｜中-大）——**已完成 2026-09-06**（spec `docs/planning/specs/2026-09-06-p6-expression-eval-subset.md` 状态已回写；三个取舍点经用户确认：属性约定降级、自研 parser 不引 Roslyn——Roslyn 绑定模型面向 Compilation 无法绑活动对象、根名与 debug_variables 同源）。落地三层：Engine `DebugEngineCore.EvaluatePathAsync`（命令泵内逐段直读绕开 MaxChildren=32 截断；属性约定降级 X→_x→_X→`<X>k__BackingField` + 全基类链字段枚举；`$exception` 伪根 + slotN 回退）；Session `Evaluation/`（`ExpressionParser` 自研递归下降——语法面即规格，能解析即已支持；`ExpressionEvaluator` AST 调度：路径进引擎、`!`/比较本层标量化——数值统一 decimal、字符串 Ordinal、布尔与 null 仅 ==/!=）；宿主 `debug_evaluate` 工具（`DebugInspectTool.TryRequireStopped`/`RenderVariable` 放宽 internal 复用）。踩坑实录：① `thread.CurrentException` 无在抛异常时 ClrDebug 抛 `S_FALSE` 须兜住（P2 同款）；② engine 测试 attach 发生在 Main 已执行后 → Main 入口断点不可达，锚点须用 delay 后的方法（DebugTarget 为此新增 `WorkScores(int[] scores, int n)` 数组目标，bag 模式串行调用）；③ v1 文法无括号，`!(a==b)` 报错指引。测试：parser 47 纯单测（合法/非法穷尽）+ 求值语义 15 单测（免进程）+ Engine 集成 1 + 宿主 e2e 1；全量 Decompiler 21/Engine 17/Session 76/Web 18/宿主 433/Client 全绿。P7 条件断点挂接点：`ExpressionEvaluator.EvaluateAsync` + `ScalarValue is bool`（求值失败/非 bool = 不命中）。
- [x] **P7 条件断点**（Session+宿主｜中，依赖 P6）——**已完成 2026-09-06**（spec `docs/planning/specs/2026-09-06-p7-conditional-breakpoints.md` 状态已回写；两个取舍点经用户确认：条件先于计数、失败 consume 式反馈）。落地：Engine 定义 `IBreakpointConditionEvaluator` + `PathValueResolver` 小接口（依赖倒置解「求值器在 Session、求值必须在泵内」的层向矛盾），`HandleBreakpoint` 条件先判（false/失败放行不 RegisterHit；失败发 `BreakpointConditionFailed` 事件）；Session `ExpressionConditionEvaluator` 单例（解析+同步核求值，PathNode 用引擎传入的 `ReadPathValue` 直读；红线=泵线程内纯计算不得再入）+ `ExpressionEvaluator.EvaluateCore` 同步核抽取（P6/P7 共用一份比较逻辑，async 门面改投泵阻塞等待）；SessionEventBuffer consume 式折叠（次数+最近断点+错误）；宿主 `debug_breakpoint_set` 加 `condition`（三种定位通用，set 时 parser 当场拒语法错）、list 显示条件+条件为真次数、`debug_state`/`debug_wait` 反馈「条件未通过 N 次」。踩坑实录：① 测试辅助方法内 `using var target` 会在返回时 Dispose 进程对象——后续 `HasExited` 抛 "No process is associated"（BreakpointTests 把进程放测试顶层正是为此）；② 引擎段报错用 `.Missing`（段路径）非 `b.Missing`（全路径）。测试：Session 桩单测 10 + Engine 桩集成 3（false 不停不计数/true 停/失败发事件）+ 宿主 e2e 1（`i == 2` 第 3 轮才停、失败反馈 2 次、语法错拒绝）；全量 Decompiler 21/Engine 20/Session 86/Web 18/宿主 434/Client 全绿。
- [x] **P8 debug_processes 进程发现**（Session+宿主｜很小）——**已完成 2026-09-06**（方案一句话已按清单预批执行）。实现：Engine 新增 `ClrProcessFinder`——`Process.GetProcesses` 枚举 + dbgshim `EnumerateCLRs` 逐 pid 探测 CLR（可附加的权威判定；非 .NET 进程快速失败跳过；**调试器自身排除**防 attach 自身死锁；成功后 `CloseCLREnumeration` 释放句柄）；列 pid/进程名/CLR 版本（自版本串提取）。宿主 `debug_processes(filter="")` 工具。查证实录：ClrDebug `HRESULT` 是枚举无比 bool、结果项属性名是 `Path` 非 ClrPath——均以本地克隆为准修正。Engine 15/宿主 432 全绿（命令行无法跨进程读取，v1 不展示——诚实省略）。
- [x] **P9 launch 早期接管**（Engine/Session+宿主｜小-中）——**已完成 2026-09-06**（方案经 spike 实测+用户确认，比原设想缩窄）。spike 两个实锤：① dbgshim 原生 `CreateProcessForLaunch` **不支持输出重定向**（会污染 MCP stdio，原生 launch 路径此路不通）；② `RegisterForRuntimeStartup` 蹲守 CLR 启动（回调=Main 前）+ 自起进程 + 标准 attach 在**零延迟目标**上断点命中（接管在 Main 前的决定性证明）。落地：`LaunchAndAttachAsync` 以蹲守替换固定 1s（新增 timeoutSeconds 参数贯通 debug_launch；超时/提前退出可诊断提示，注册超时即 Unregister 清理）——`debug_launch`「目标需自带启动延迟」约束解除，参数形态不变（初始断点清单参数不需要：launch 返回即冻结在 Main 前，agent 从容设断点走既有 pending 机制）。spike 测试转正为时序护栏（`LaunchRegisterStartupSpikeTests`）；Session/Engine AGENTS launch 时序描述同步。Decompiler 21/Engine 15/Session 14/宿主 432/Web 18/Client 全绿。

## agent 实战反馈问题清单（2026-09-06 收到 agent 使用报告；R1-R4/R7/R8 已完成并合并 master，R5 决策跳过转 ROADMAP，R6 spec 待评审）

> 来源：一个 agent 实际用本 MCP 调试 ASP.NET Web 应用后的 8 条反馈，逐条源码核实均为属实；修复前中大型项先立 spec、小型项补方案段再动码（同上规矩）。
> **2026-09-06 落地**（分支 fix/r1-r8-agent-feedback）：R1/R2/R3/R4/R7/R8 已修（详见各条「落地」注）；R5/R6 中-大项留待 spec。DebugTarget 生成脚本新增 cwd/env 打印（供 R1 测试断言）；`ProcessOutputCapture` 上限 500→2000。实测：Session 全过、宿主 ArgumentValidators/Concurrency/CmdTests 过；DebugMcpToolsTests 本机 5 失败为环境时序问题（CI master 基线 success 确认非代码回归）。
> **2026-09-07 已合并 master**（dac7aa8，ff-only）。**R5 决策（2026-09-07）**：跳过转 ROADMAP v2 候选（agent 主导下状态机不可读非问题；.NET 11 runtime-async 查证对 V1 无运行时改进；L3 语义单步重量不匹配），spec 标 Superseded 保留作记录。**R6 spec 草案已立**：`docs/planning/specs/2026-09-07-r6-line-breakpoint-deferred.md`（待评审，取舍点见 spec §7）。
> **遗留排查（未结）**：DebugMcpToolsTests 本机 5/8 失败（clean master + 原始 exe 同样失败；同一提交远程 CI green，判定非代码回归）——失败模式为 `debug_breakpoint_set` continue 后返回「断点已登记」（pending）而测试期望「已设」，疑似 ICorDebug module-load 竞态在慢负载本机上的表现；待与 Web 服务相关性对照实验（见下「本机 DebugMcpToolsTests 5 失败排查」）。

- [ ] **本机 DebugMcpToolsTests 5 失败排查**（宿主测试｜环境/时序）——**现象**：8 个动态调试 e2e 测试本机失败 5 个（其余 3 过），失败断言均在「设断点 → continue → 期望断点已设/命中」环节，返回「断点已登记（pending）」；clean master HEAD + 原始 exe 复现同样失败，远程 CI 对 master（workflow_dispatch run 34037548674）success——**判定非 R1-R8 代码回归**。**对照实验（2026-09-07，已做完）**：无 Web 基线跑 = 5 失败；启动 BootstrapBlazorApp1.Server（E:\Code\Test\...，监听 :5000）+ 120 请求压力跑 = **同样 5 失败、失败集合一致**——**与 Web 服务无关坐实**（DebugMcpToolsTests 全为 engine 自 launch TestSamples.exe 的 ICorDebug 闭环测试，源码无任何 web/Blazor/Kestrel 引用；碰 Web 的 McpWebCoexistTests 不在失败名单）。**待办**：① 本机复跑确认竞态窗口（是否偶发、失败集合是否漂移）；② 若确为 module-load 竞态：考虑像 P9 首次 continue 前重扫补绑（8b7e806 同款自愈）那样的时序加固或测试侧等待模块绑定再断言。

- [x] **R1 launch 缺工作目录/环境变量参数**（宿主+Session｜小）——**已核实属实**。`DebugSessionManager.LaunchAndAttachAsync` 的 `ProcessStartInfo` 未设 `WorkingDirectory`/环境变量，子进程继承 MCP server 的 CWD（`UseShellExecute=false`），Web 应用 ContentRoot 落到 server 目录→ wwwroot 404、`ASPNETCORE_ENVIRONMENT` 缺失走非 Development 分支。**实测补证（2026-09-06，BootstrapBlazorApp1.Server）**：MCP server 为目标进程父进程，CWD 直传目标；该工程用静态 WebAssets（bin 无物理 wwwroot，manifest 从源目录映射虚拟资源），故 wwwroot 404 不显形，真病灶在 appsettings/环境名（`WebApplication.CreateBuilder` 以 `Directory.GetCurrentDirectory()` 当 ContentRoot）——launch 后 Kestrel 监听 5000 而非 launchSettings 的 5220，印证启动参数/环境缺失。修复方向：`debug_launch` 加 `workingDirectory=""`/`environment=""`（KEY=VALUE 多行/分号组合，均带默认值）贯通到 psi；README 同步。 **落地**：`debug_launch` 加 `workingDirectory=""`/`environment=""`（KEY=VALUE 多行/分号，均带默认值），Session `LaunchAndAttachAsync` 贯通 psi（不存在的工作目录当场报错）；README 动态调试表+参数详表同步。
- [x] **R2 会话不显示目标 pid**（宿主｜小）——**已核实属实**。`debug_state` 输出只有 会话状态/最近停点，无 pid/目标行（`DebugSessionTool.DebugState`）；`debug_processes` 也不标注「哪个是当前会话目标」（`DebugProcessTool.DebugProcesses`）；`ActiveDebugSession` 持有 `_process` 但未暴露 pid。**实测补证**：launch 返回与 debug_state 均无 pid/无实际监听地址——agent 想开浏览器/查端口无据可依（launch 直接 spawn exe 绕过 dotnet-run，launchBrowser/applicationUrl 不生效，浏览器本就不会被目标拉起）。修复方向：launch/attach 返回与 debug_state 头部加 pid（launch 在 Manager 里已拿到 process.Id，attach 本来就是 pid 入参）+ 实际监听 url（如能从输出/环境推得）；debug_processes 对当前会话目标行加「← 当前会话」标注。 **落地**：`ActiveDebugSession` 暴露 `ProcessId`（launch=自起进程 Id，attach=入参 pid），launch 返回与 debug_state 头部加「目标 pid」；debug_processes 对当前会话目标行标注「← 当前会话」（顺带进程名排序+超 100 截断，见 R8②）。
- [x] **R3 Attaching 状态含义不明**（宿主｜小）——**已核实属实**。attach 引导完成后 `PublishState(Attaching, "attached")`（`DebugEngineCore.DoBootstrap`），此后进程被初始 hold 冻在 Main 前、状态一直停留 Attaching 直到首次 continue（`StateText` 对 Attaching 无映射、裸吐英文枚举名）；agent 无法区分「还在等 CLR」与「已冻结在 Main 前可设断点」，误判后先 continue 直接错过入口。**实测补证**：launch 后连续多次 debug_state 均只吐 `会话状态: Attaching`，无任何可操作提示；且 launch 返回 `当前状态：None`、state 显示 Attaching，前后状态名不一致加剧困惑。修复方向：StateText 补 Attaching/Launching 中文映射并注明「进程已冻结在 Main 前，可直接设断点（含 pending），debug_continue 放行」；launch 返回文案已说明，state 侧补齐即可。 **落地**：`StateText` 补 Launching/Attaching 中文映射——Attaching=「进程冻结在 Main 前，可设断点后 debug_continue 放行」，Launching=「等待 CLR 初始化完成，进程尚冻结」。
- [x] **R4 step 返回文案与异步语义不符**（宿主｜小）——**已核实属实**。引擎单步是投泵异步命令（`StepAsync` post 后立即 `PublishState(Running)` 返回，停点稍后经 StepCompleted 事件到达），而 `DebugStep` 返回文案说「单步完成进程停下」——误导 agent 以为停下；并发连发 step 时后续调用撞上「当前非 Stopped」报错，被迫 step→wait 串行，每步 ≥2 往返。修复方向（二选一或并用）：① 文案改为「单步命令已提交，进程将停在新位置——用 debug_wait 等停（通常瞬时）」；② 考虑 step 门面内联等停（如内部接 debug_wait 短超时）减少往返，需防死锁/超时语义变化。 **落地**：`DebugStep` 返回文案改「已提交 step 命令——用 debug_wait 等停（通常瞬时）或 debug_state 确认 Stopped」，Description 同步。
- [x] ~~**R5 async 状态机单步体验差**（Engine｜中-大）~~——**决策（2026-09-07）：跳过，登记为未来计划**。三条理由：① agent 主导——agent 读栈消化 token/MoveNext 语义，栈帧机械性对 agent 是低噪声非不可读；且 agent 调试 async 的正确工作流是断点直达 + trace/条件断点，非逐步走查；② .NET 11 查证（epic dotnet/runtime#109632 runtime-async/Async V2）——V2 让 async 方法**不再生成** `<Method>d__N.MoveNext`（栈天然干净），但需目标代码 net11+opt-in 重编译才生效，**对 V1 状态机（存量代码）无任何运行时级可读性改进**，V1 帧过滤永远是调试器侧自己的活；③ 实现重量评估——L1（停点标注+帧名真名化）轻，L2（无 PDB 启发式）中低，L3（跨 await 语义单步，dnSpy DbgEngineStepperImpl 全套）重量在「跨挂起点 continuation 追踪」需隐藏断点/对象 id/反编译 async debug info 三方底座，1-2 周+，与 agent 工作流不匹配。原 spec `docs/planning/specs/2026-09-07-r5-async-step.md` **不实施**（保留作记录，见 docs/ROADMAP.md v2 候选新增 async 单步语义项 + V2 观察）。
- [ ] **R6 行断点模块加载前不可用**（宿主+Session｜中）——**已核实属实（现状即如此，且已有缓解提示）**。token 方式支持 pending 待绑定（`BreakpointManager` 重绑机制），而 typeName+line/sourcePath+line 两种行定位要求模块已加载（`ResolveModuleForLineAsync`/`SetBySourceLineAsync` 直接报「未加载」），报错文案已附 `LineBreakpointRetryHint` 引导改用 token 或 continue 后重试。修复方向：行断点支持「延迟解析登记」——记住 typeName/sourcePath+line，在 TrackModule/首次 continue 重扫时对新增模块重试行→token 解析并绑定（复用 P9 竞速自愈点）；注意宿主层需拿到 DocumentService/PDB 解析器注入引擎回调的层向（可能经 Session 中转）。 **spec 已立**：`docs/planning/specs/2026-09-07-r6-line-breakpoint-deferred.md`（草案待评审）——引擎 LoadModule 无条件发 ModuleLoaded 事件（现状仅 rebound>0 才发 BreakpointsChanged）、宿主工具层登记延迟项、订阅模块加载自动补设（查重防重）。三个取舍点待评审（见 spec §7）。
- [x] **R7 list_types 参数名 list 可发现性差**（宿主+Decompiler｜小，破坏性）——**已核实属实**。参数名 `list`、值为 "csi" 组合（`ListTypesTool.ListTypes`），agent 猜 `kind` 报错。修复方向：加别名参数（如 `categories`）或改名（改名=破坏性变更，README/缓存签名 `CacheSignatures.ListTypes` 同步；MCP 工具改名属大动作，建议先加 description/报错提示引导，报错文案里列出合法参数名与示例）。 **落地（破坏性改名）**：参数 `list` → `categories`（用户确认允许破坏性修复），同步 CLI 命名参数调用、`ArgumentValidators` 报错文案、测试/Client 参数键、README。
- [x] **R8 输出/进程列表噪声**（宿主+Session｜小-中）——**已核实属实**。① `debug_output` 环形缓冲上限 500 行（`ProcessOutputCapture.MaxLines`），Web 应用启动轮询日志会冲掉早期关键行（Now listening 等），`lines` 参数最大也只有 500——方向：提高上限或加 grep 过滤参数（`filter=""`）/按流过滤，缓冲提高后注意内存；**实测补证**：Web 应用每 ~100ms 一条请求日志，10 秒即刷满 500 行环形缓冲，两次 debug_output 都只能回溯到启动后 ~15 秒，启动早期（Content root/Now listening/环境名）彻底不可见——本项对 Web 调试是硬伤；② `debug_processes` 无 filter 时一次列 217 个 .NET 进程且不标注当前会话目标（与 R2 同修），方向：列表加进程名排序+当前目标标注，必要时超 N 条截断提示用 filter。 **落地**：① `ProcessOutputCapture.MaxLines` 500→2000，`Tail` 加 `filter`（尾部向上取命中行），`debug_output`/`debug_wait` 加 `filter`/`outputFilter` 筛关键行（实测 filter 抓到 env/cwd 行）；② debug_processes 进程名排序+「← 当前会话」标注+超 100 截断提示用 filter。
