# TODO（DotNetDebuggerMcp 宿主近期待办）

> 近期待办，完成一项删一项；远期想法见 `docs/ROADMAP.md`；开发指南见同目录 `AGENTS.md`。

- [ ] **`web_open` 幂等 MCP 工具**：agent 按需开 Web 可视化；MCP server 默认不再带 `--web`。牵动 `--web` 启动分支拆分 + 新增工具（参数默认值/中文 Description 铁律适用）+ 根 README 同步。根 `opencode.json` 现带 `--web` 仅为联调。
