# 实施计划 · V3 日志+事件统一时间线（debug_timeline）

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:executing-plans 逐 Task 实施（或 subagent-driven-development 逐 Task 派发）。Step 用 `- [ ]` 追踪。

**Goal:** 落地统一时间线：Session 补「调试事件历史环形缓冲 + 归并渲染」，宿主加 `debug_timeline(filter, kind, lines)`，按 UTC 时间对齐输出 目标日志 ↔ 调试事件 ↔ agent 动作，解「先 A 日志→命中 B 断点→agent 改值→再 C 异常」复盘拼图；随带 V3 顺手项（launch 退出码显示 + attach 即成功提示）。

**Architecture:** Session 侧纯内存：`SessionEventBuffer` 在既有折叠逻辑外把关键 `DebugEvent` 另存 500 条历史（Engine 零改动）；新 `DebugTimeline` 静态归并（日志 `ProcessOutputCapture` + 事件历史 + 动作 `AgentActionLog` 三源，按 Utc 主序 + 源内 Sequence 次序，filter/kind/maxLines 后返回统一行）。宿主 `debug_timeline` 拉三源调用归并渲染；`debug_state` 补 Exited 退出码与 attach 即成功提示。

**Tech Stack:** C# net10.0、ModelContextProtocol.Server；复用现成 record：`ProcessOutputLine`/`AgentAction`/`DebugEvent`（含各 Payload）。

**Spec:** `docs/planning/specs/2026-09-08-v3-timeline.md`（2026-09-09 已拍板转正：500 条含 EngineLog + 动作入 + TraceHit 双轨 + 不折叠 + 退出码/attach 提示随 V3）。

## Global Constraints（根 AGENTS.md + Session/宿主 AGENTS.md 铁律）

- 所有 MCP 工具参数带默认值（`string x = ""`，不声明可空）；`[Description]` 中文、面向 agent、注明默认值；工具带 `CancellationToken cancellationToken = default`；返回 `Task<string>`，一切错误中文提示不抛异常。
- 改 MCP 工具必须同 commit 改根 `README.md`；新使用者可见变更同批记 `CHANGELOG.md` `[Unreleased]`。
- Session `DebugSessionManager` 是共享单例（宿主/Web 同一实例），勿各处 new；`ActiveDebugSession.Actions` = `Manager.Actions`（同一 `AgentActionLog`，跨会话累积）。
- SessionEventBuffer 消费线程纪律：历史追加在 `OnEvent` 内、与状态折叠同锁串行，不新增后台任务。
- 测试先跑 `generate-testdata.ps1`；Session.Tests 真实 attach 串行（`ParallelMode.None`），纯内存测试（历史/归并）可快跑不受限。
- 输出约定：文本行带行号分页面向反编译元数据；debug_* 工具沿用其自有「头部信息块 + 行列表」形态，不引入反编译行号分页语义。

---

### Task 0: 工作区/分支准备（提交前确认）

- [ ] **Step 1**: 与用户确认实施分支策略（master 直接实施 或 新建 feature 分支）。
- [ ] **Step 2**: 跑 `generate-testdata.ps1`；`dotnet build -c Release src/DotNetDebuggerMcp/DotNetDebuggerMcp.csproj` 基线绿。

---

### Task 1: Session — 事件历史环形缓冲 + 消费线程测试接缝

**Files:**
- Modify: `src/DotNetDebugger.Session/SessionEventBuffer.cs`（`Start:98`、`OnEvent:151`）
- Test: `tests/DotNetDebugger.Session.Tests/SessionEventBufferTests.cs`

**Interfaces:**
- Consumes: `DebugEvent`/`DebugEventKind`（Engine Models，事件自带 `UtcTimestamp`/`Sequence`）。
- Produces: `SessionEventBuffer.MaxHistory = 500`（const）；`SnapshotHistory()` → `IReadOnlyList<DebugEvent>`（旧→新，超 500 丢最旧）；public `Start(DebugSession)`（后台 fire-and-forget 消费，现语义不变）+ internal `Feed(IEnumerable<DebugEvent>)`（同步遍历即 OnEvent，纯内存测试用——调用后断言无竞态）。

- [ ] **Step 1: 写失败测试（纯内存）**

`SessionEventBufferTests` 加两例（含真 attach 的既有用例不动）：
```csharp
[Fact]
public async Task History_KeepsKeptKindsAndDropsOldestBeyondCap()
{
    await using var buffer = new SessionEventBuffer();
    var events = new List<DebugEvent>();
    // 投喂 MaxHistory+5=505 条 BreakpointHit + 1 条 EngineLog（共 506）：500 上限挤掉最早 6 条 BP，EngineLog 最后入队必保留
    for (var i = 0; i < SessionEventBuffer.MaxHistory + 5; i++)
        events.Add(new DebugEvent("s", i, DateTimeOffset.UtcNow.AddMilliseconds(i), DebugEventKind.BreakpointHit, null));
    events.Add(new DebugEvent("s", 10_000, DateTimeOffset.UtcNow, DebugEventKind.EngineLog, new EngineLogPayload("info", "hello")));
    buffer.Feed(events);                                         // internal 同步接缝：返回后断言无竞态
    var snap = buffer.SnapshotHistory();
    Assert.Equal(SessionEventBuffer.MaxHistory, snap.Count);             // 环形上限
    Assert.Contains(snap, e => e.Kind is DebugEventKind.EngineLog);      // 最新 EngineLog 保留
    Assert.Equal(6, snap[0].Sequence);                 // 最早保留 seq=6（seq 0..5 六条 BP 被挤掉）
    Assert.Equal(10_000L, snap[^1].Sequence);          // 最新一条是 EngineLog
    for (var i = 1; i < snap.Count; i++) Assert.True(snap[i].Sequence >= snap[i - 1].Sequence); // 按 Sequence 升序
}

[Fact]
public void History_OnlyKeepsConfiguredKinds()
{
    await using var buffer = new SessionEventBuffer();
    // 被排除 kind（BreakpointConditionFailed/BreakpointsChanged/ThreadsChanged）不入历史 → 快照为空
    buffer.Feed(new DebugEvent[] {
        new("s", 1, DateTimeOffset.UtcNow, DebugEventKind.BreakpointConditionFailed, new BreakpointConditionFailedPayload(1, 1, "e")),
        new("s", 2, DateTimeOffset.UtcNow, DebugEventKind.BreakpointsChanged, new BreakpointsChangedPayload([])),
        new("s", 3, DateTimeOffset.UtcNow, DebugEventKind.ThreadsChanged, null),
    });
    Assert.Empty(buffer.SnapshotHistory());
}
```
Run `dotnet test --project tests/DotNetDebugger.Session.Tests/... --filter History_`，Expected: FAIL（`SnapshotHistory`/`MaxHistory` 不存在）。

- [ ] **Step 2: 实现历史环形 + 接缝**

`SessionEventBuffer.cs`：
1. 加字段：`public const int MaxHistory = 500;`、`private readonly Queue<DebugEvent> _history = new();`（锁内访问）。
2. 消费入口拆分（**public 生产语义不变；internal 接缝 await 后确定性完成**）：
```csharp
/// <summary>后台消费事件流（生产路径；fire-and-forget，语义同现状）。</summary>
public void Start(DebugSession session)
{
    _cts = new CancellationTokenSource();
    _consumer = Task.Run(async () =>
    {
        try { await foreach (var e in session.Events.WithCancellation(_cts.Token)) OnEvent(e); }
        catch (OperationCanceledException) { }
        catch { /* 会话关闭后事件流结束 */ }
    });
}
/// <summary>同步喂入事件序列（internal，纯内存测试用：遍历即 OnEvent，返回后断言无竞态）。</summary>
internal void Feed(IEnumerable<DebugEvent> events)
{
    foreach (var e in events) OnEvent(e);
}
```
3. `OnEvent` 开头（`prevState` 读取之后、`switch` 之前）追加历史：
```csharp
AppendHistory(e);
```
```csharp
private void AppendHistory(DebugEvent e)
{
    if (!IsHistoryKind(e.Kind)) return;
    lock (_gate)
    {
        _history.Enqueue(e);
        while (_history.Count > MaxHistory) _history.Dequeue();
    }
}
private static bool IsHistoryKind(DebugEventKind k) => k is
    DebugEventKind.SessionStateChanged or DebugEventKind.BreakpointHit or
    DebugEventKind.StepCompleted or DebugEventKind.ExceptionHit or
    DebugEventKind.ExceptionSkipped or DebugEventKind.TraceHit or DebugEventKind.EngineLog;
```
> 注：TraceHit 历史存原 `DebugEvent`（载荷对象引用共享，非复制字符串；`_traces` 消费式语义不动——双轨并存）。
4. 快照：
```csharp
/// <summary>事件历史快照（旧→新；debug_timeline 事件源）。</summary>
public IReadOnlyList<DebugEvent> SnapshotHistory()
{
    lock (_gate) return _history.ToArray();
}
```
5. 若 Session 库尚无 `[assembly: InternalsVisibleTo("DotNetDebugger.Session.Tests")]`，在 csproj/AssemblyInfo 补（internal `Start` 重载与测试同程序集可见）。

- [ ] **Step 3: 运行测试**

`SessionEventBufferTests` 全部（含既有真 attach 用例）过；既有 `WaitForStop_RealBreakpointHit_...` 不受影响。`git commit -m "feat: SessionEventBuffer 事件历史环形 500 条（含 EngineLog，供 debug_timeline，V3）"`

---

### Task 2: Session — DebugTimeline 三源归并渲染（纯内存）

**Files:**
- Create: `src/DotNetDebugger.Session/DebugTimeline.cs`
- Modify: `src/DotNetDebugger.Session/ProcessOutputCapture.cs`（加 `Snapshot()`）
- Test: `tests/DotNetDebugger.Session.Tests/DebugTimelineTests.cs`

**Interfaces:**
- Consumes: `ProcessOutputCapture.Snapshot()`（Task 2 新增）、`SessionEventBuffer.SnapshotHistory()`、`AgentActionLog.Snapshot()`；payload record 字段（`BreakpointHitPayload`/`StepCompletedPayload`/`ExceptionHitPayload`/`ExceptionSkippedPayload`/`TraceHitPayload`/`SessionStateChangedPayload`/`EngineLogPayload`，Engine Models）。
- Produces:
  - `ProcessOutputCapture.Snapshot()` → `IReadOnlyList<ProcessOutputLine>`（全量，旧→新；不动 `Tail` 语义）。
  - `TimelineRow(DateTimeOffset Utc, long Seq, string Tag, string Text)`（Session Models）。
  - `DebugTimeline.Build(IReadOnlyList<ProcessOutputLine> logs, IReadOnlyList<DebugEvent> history, IReadOnlyList<AgentAction> actions, int maxLines = 100, string? filter = null, string kind = "all")` → `IReadOnlyList<TimelineRow>`（时间主序 + 次序列稳定，取最近 maxLines；**kind 校验清单由宿主工具内联 `validKinds` 持有，本层不重复定义**）。

- [ ] **Step 1: 写失败测试（纯内存）**

`DebugTimelineTests.cs`：
```csharp
[Fact]
public void Build_MergesThreeSourcesChronologically()
{
    var logs = new[] { new ProcessOutputLine(1, T("2026-09-09T12:00:01"), ProcessOutputStream.Stdout, "listening :5000") };
    var history = new[] { new DebugEvent("s", 1, T("2026-09-09T12:00:03"), DebugEventKind.BreakpointHit,
        new BreakpointHitPayload(3, 8, null)) };
    var actions = new[] { new AgentAction(1, T("2026-09-09T12:00:02"), "debug_evaluate", "i == 3", "ok") };
    var rows = DebugTimeline.Build(logs, history, actions, maxLines: 100);
    Assert.Equal(3, rows.Count);
    Assert.Equal(["log", "act", "bp"], rows.Select(r => r.Tag));      // 时间升序
    Assert.Contains(rows, r => r.Text.Contains("listening"));
    Assert.Contains(rows, r => r.Text.Contains("bp3"));               // 事件摘要
    Assert.Contains(rows, r => r.Text.Contains("debug_evaluate"));
}
[Fact] public void Build_FilterSubstring_OnlyKeepsMatches() // filter="WorkScores"
[Fact] public void Build_KindSelectsSource()                    // kind="bp"/"log"/"act"/"state"/"engine"/"trc"…
[Fact] public void Build_TakesLastMaxLinesChronological()       // maxLines 截最近 N 且仍时间升序
[Fact] public void Build_SameMillisecondStableBySeq()           // 同 Utc 时按 TagRank+Seq 稳定
```
Run，Expected: FAIL（类型不存在）。`T(string)` = `DateTimeOffset.Parse(..., CultureInfo.InvariantCulture, DateTimeStyles.RoundtripKind)` 本地测试助手。

- [ ] **Step 2: 实现（ProcessOutputCapture.Snapshot + DebugTimeline）**

`ProcessOutputCapture.cs` 加：
```csharp
/// <summary>全量快照（旧→新；debug_timeline 归并用，Tail 保留给既有语义）。</summary>
public IReadOnlyList<ProcessOutputLine> Snapshot()
{
    lock (_gate) return _lines.ToArray();
}
```

`DebugTimeline.cs`（静态类）：
```csharp
public sealed record TimelineRow(DateTimeOffset Utc, long Seq, string Tag, string Text);

/// <summary>统一时间线行渲染：三源合并（时间主序；同毫秒用 TagRank+Seq 稳定次序）。
/// 行文本格式：[HH:mm:ss.fff] tag 内容（tag 两字母：log/act/bp/step/exc/skip/trc/state/engine）。</summary>
public static class DebugTimeline
{
    private static readonly string[] TagOrder = ["log", "act", "bp", "step", "exc", "skip", "trc", "state", "engine"];

    public static IReadOnlyList<TimelineRow> Build(
        IReadOnlyList<ProcessOutputLine> logs, IReadOnlyList<DebugEvent> history,
        IReadOnlyList<AgentAction> actions, int maxLines = 100, string? filter = null, string kind = "all")
    {
        var rows = new List<TimelineRow>();
        if (kind is "all" or "log")
            foreach (var l in logs)
                rows.Add(new TimelineRow(l.Timestamp, l.Sequence, "log", l.Text));
        if (kind is "all" or "act")
            foreach (var a in actions)
                rows.Add(new TimelineRow(a.UtcTimestamp, a.Sequence, "act", $"{a.Tool} {a.ArgsSummary}".Trim()));
        if (kind is "all" or "bp" or "step" or "exc" or "skip" or "trc" or "state" or "engine")
            foreach (var e in history)
            {
                var tag = TagOf(e.Kind);
                if (tag is null || (kind != "all" && tag != kind)) continue;
                rows.Add(new TimelineRow(e.UtcTimestamp, e.Sequence, tag, Summarize(e)));
            }

        var seq = 0; // 归并后稳定序号：同 Utc 时按 TagOrder 序号 + 源 Sequence
        var sorted = rows
            .OrderBy(r => r.Utc)
            .ThenBy(r => Array.IndexOf(TagOrder, r.Tag))
            .ThenBy(r => r.Seq)
            .ToList();
        if (!string.IsNullOrWhiteSpace(filter))
            sorted = sorted.Where(r => r.Text.Contains(filter.Trim(), StringComparison.OrdinalIgnoreCase)).ToList();
        var skip = Math.Max(0, sorted.Count - Math.Max(1, maxLines));
        return sorted.Skip(skip).ToArray();
    }

    private static string? TagOf(DebugEventKind k) => k switch
    {
        DebugEventKind.BreakpointHit => "bp",
        DebugEventKind.StepCompleted => "step",
        DebugEventKind.ExceptionHit => "exc",
        DebugEventKind.ExceptionSkipped => "skip",
        DebugEventKind.TraceHit => "trc",
        DebugEventKind.SessionStateChanged => "state",
        DebugEventKind.EngineLog => "engine",
        _ => null, // BreakpointConditionFailed/ThreadsChanged/BreakpointsChanged 不入历史不渲染
    };

    private static string Summarize(DebugEvent e) => e.Kind switch
    {
        DebugEventKind.SessionStateChanged when e.Payload is SessionStateChangedPayload sp => sp.State.ToString() + (string.IsNullOrEmpty(sp.Reason) ? "" : $"（{sp.Reason}）"),
        DebugEventKind.BreakpointHit when e.Payload is BreakpointHitPayload bp =>
            $"bp{bp.BreakpointId} @ {Loc(bp.TopFrame)} (t={bp.ThreadId})",
        DebugEventKind.StepCompleted when e.Payload is StepCompletedPayload sc => $"step {sc.Reason} @ {Loc(sc.TopFrame)} (t={sc.ThreadId})",
        DebugEventKind.ExceptionHit when e.Payload is ExceptionHitPayload ex =>
            $"exception {ex.ExceptionType}" + (string.IsNullOrEmpty(ex.Message) ? "" : $": {ex.Message}") + $" @ {Loc(ex.TopFrame)}",
        DebugEventKind.ExceptionSkipped when e.Payload is ExceptionSkippedPayload sk => $"skip {sk.ExceptionType}（过滤器放行）",
        DebugEventKind.TraceHit when e.Payload is TraceHitPayload th =>
            $"bp{th.BreakpointId} t={th.ThreadId} {string.Join(" ", th.Variables.Take(3).Select(v => $"{v.Scope}.{v.Name ?? "slot" + v.Slot}={v.Display}"))}" + (th.Variables.Count > 3 ? " …" : ""),
        DebugEventKind.EngineLog when e.Payload is EngineLogPayload el => $"[{el.Level}] {el.Message}",
        _ => e.Kind.ToString(),
    };

    private static string Loc(FrameLocation? f) => f is null ? "?" : $"{f.ModuleName}!{f.MethodTokenText}+0x{f.IlOffset:x}";
}
```
> 帧位置文本借用引擎既有 `FrameLocation` 展示约定（`MethodTokenText` 字段名以 `Models/FrameLocation.cs` 为准，若属性不同按实际替换——`Loc` 语义=模块!token+IL offset 即可）。

- [ ] **Step 3: 运行测试 + 提交**

Run Session `DebugTimelineTests` + `ProcessOutputCaptureTests`（Tail 语义未动）+ 既有 Session 全量。`git commit -m "feat: Session DebugTimeline 三源归并（log/bp/step/exc/skip/trc/state/engine/act，V3）"`

---

### Task 3: Session — ActiveDebugSession 退出码与 attach 标记

**Files:**
- Modify: `src/DotNetDebugger.Session/DebugSessionManager.cs`（`ActiveDebugSession:10`、`LaunchAndAttachAsync` Exited 处理 `:126`、`AttachAsync:78`、`Activate:200`）
- Test: `tests/DotNetDebugger.Session.Tests/DebugSessionManagerTests.cs`

**Interfaces:**
- Consumes: `process.Exited` 事件（launch 路径已挂）；`ActiveDebugSession` 构造参数。
- Produces: `ActiveDebugSession.IsAttach`（bool）与 `ActiveDebugSession.ExitCode`（int?，launch 捕获、attach=null）。

- [ ] **Step 1: 写失败测试**

`DebugSessionManagerTests.cs` 加（launch 短活目标，等 Exited 状态后断言）：
```csharp
[Fact]
public async Task LaunchAndAttach_ExitCodeCapturedOnExited()
{
    await using var manager = new DebugSessionManager();
    var active = await manager.LaunchAndAttachAsync($"{TestTarget.DebugTargetExe} 1 0", ct: TestContext.Current.CancellationToken);
    Assert.False(active.IsAttach);
    await active.Session.ContinueAsync(TestContext.Current.CancellationToken); // launch 返回时冻结在 Main 前——不放行进程永不退出，ExitCode 恒 null
    // 等进程退出（输出缓冲有 [进程已退出 exitCode=N] 标记）；active.ExitCode 由 process.Exited 捕获
    var deadline = DateTime.UtcNow.AddSeconds(15);
    while (DateTime.UtcNow < deadline && active.ExitCode is null) await Task.Delay(100, TestContext.Current.CancellationToken);
    Assert.NotNull(active.ExitCode);
}

[Fact]
public async Task Attach_MarksIsAttachAndNoExitCode()
{
    using var target = TestTarget.StartDebugTarget("2 4"); // 真实 attach 慢用例放既有模式后
    // attach 后断言 active.IsAttach == true 且 active.ExitCode is null
}
```
Run，Expected: FAIL（属性不存在）。

- [ ] **Step 2: 实现**

`ActiveDebugSession` 加属性与构造参数：
```csharp
/// <summary>是否 attach 建立（attach 会话拿不到退出码、无输出捕获）。</summary>
public bool IsAttach { get; }
/// <summary>launch 会话的目标退出码（进程退出后由 process.Exited 捕获）；attach 会话恒 null。</summary>
public int? ExitCode { get; internal set; }
```
`internal ActiveDebugSession(..., bool isAttach = false)` 存 `IsAttach = isAttach;`。

`DebugSessionManager`：
1. 退出码捕获（**2026-09-09 审查修正**）：`Activate` 返回后（目标冻结在 Main 前，进程不可能已退出）注册处理器，**闭包捕获 `active`**——不在早注册的处理器里经 `Manager.Active`（会话被替换后会把旧进程退出码写到新会话，竞态且语义错）：
```csharp
var active = Activate(engineSession, $"launch+attach {commandLine}", output, process, workingDirectory: effectiveWd);
process.Exited += (_, _) =>
{
    try { if (active.ExitCode is null) active.ExitCode = process.ExitCode; } catch { /* 会话已释放忽略 */ }
    try { output.AppendSystem($"[进程已退出 exitCode={process.ExitCode}]"); } catch { }
};
return active;
```
（原 `:126` 处只保留排空读取不需要改动；Exited 标记随处理器迁移到此处。）
2. `AttachAsync` 调用 `Activate(session, $"attach pid={processId}", processId: processId, isAttach: true)`；`LaunchAndAttachAsync` 与 `LaunchAsync` 不传（默认 false）。
3. `Activate` 加 `bool isAttach = false` 参数透传构造。

- [ ] **Step 3: 运行测试 + 提交**

Session 全量（含既有 DebugSessionManagerTests）。`git commit -m "feat: ActiveDebugSession 加 IsAttach/ExitCode（launch 捕获退出码，V3 顺手项）"`

---

### Task 4: 宿主 — `debug_timeline` 工具 + debug_state 顺手项 + README/CHANGELOG

**Files:**
- Create: `src/DotNetDebuggerMcp/Tools/Debugger/DebugTimelineTool.cs`
- Modify: `src/DotNetDebuggerMcp/Tools/Debugger/DebugSessionTool.cs`（`DebugState:110` 补 Exited 退出码 + attach 即成功提示）
- Modify: `README.md`、`CHANGELOG.md`
- Test: `tests/DotNetDebuggerMcp.Tests/DebugMcpToolsTests.cs`

**Interfaces:**
- Consumes: `DebugSessionService.Manager.Active`（`active.Buffer.SnapshotHistory()`/`active.Output?.Snapshot()`/`active.Actions.Snapshot()`/`active.IsAttach`/`active.ExitCode`）；`DebugTimeline.Build`；`DebugSessionTool.StateText/StopText`（现成渲染助手）。
- Produces: MCP 工具 `debug_timeline(lines=100, filter="", kind="all")`；`debug_state` Exited/attach 增强文案。

- [ ] **Step 1: 新工具实现**

```csharp
[McpServerTool]
[Description("查看当前调试会话的日志+事件+agent动作统一时间线（按时间对齐复盘：先 A 日志→命中 B 断点→agent 改值→再 C 异常）。三源：目标进程输出（log，仅 launch 会话有）、调试事件（bp/step/exc/skip/trc/state/engine）、agent 动作（act）。lines 取最近 N 条（默认 100，v1 不做 start-end 翻页）；filter 子串过滤（忽略大小写）；kind 可选 all/log/act/bp/step/exc/skip/trc/state/engine 按源筛选。")]
public static async Task<string> DebugTimeline(
    [Description("取最近 N 条（默认 100）。")] int lines = 100,
    [Description("子串过滤（忽略大小写），只保留文本含该子串的行。")] string filter = "",
    [Description("按源筛选：all/log/act/bp/step/exc/skip/trc/state/engine。")] string kind = "all",
    CancellationToken cancellationToken = default)
{
    var active = DebugSessionService.Manager.Active;
    if (active is null) return "当前无活动调试会话。先用 debug_launch / debug_attach 建立会话。";
    var validKinds = new[] { "all", "log", "act", "bp", "step", "exc", "skip", "trc", "state", "engine" };
    var kindNorm = kind.Trim().ToLowerInvariant();
    if (!validKinds.Contains(kindNorm))
        return $"kind 无效：{kind}（可选 {string.Join("/", validKinds)}）。";
    var n = Math.Clamp(lines <= 0 ? 100 : lines, 1, 1000);

    var logs = active.Output?.Snapshot() ?? Array.Empty<DotNetDebugger.Session.ProcessOutputLine>();
    var history = active.Buffer.SnapshotHistory();
    var actions = active.Actions.Snapshot();
    var rows = DebugTimeline.Build(logs, history, actions, n, filter, kindNorm);

    var sb = new StringBuilder();
    var target = active.ProcessId > 0 ? $"目标 pid={active.ProcessId}" : "目标 ?";
    sb.Append($"时间线 {target}（共 {logs.Count} 日志 / {history.Count} 事件 / {actions.Count} 动作 → 显示 {rows.Count} 行）:");
    if (rows.Count > 0)
    {
        sb.AppendLine();
        sb.Append($"范围 {rows[0].Utc.ToLocalTime():HH:mm:ss.fff} – {rows[^1].Utc.ToLocalTime():HH:mm:ss.fff}"); // 显示统一本机时刻（源混用 Now/UtcNow 但 DTO 比较按瞬时）
    }
    // attach 即成功提示（顺手项②）：attach 到长活目标且进程在跑——不看起来像卡住
    if (active.IsAttach && active.Buffer.CurrentState == DotNetDebugger.Engine.Models.DebugSessionState.Running)
    {
        sb.AppendLine();
        sb.Append("attach 会话：已附加成功，目标进程继续独立运行（不会自行停下）；设断点后 debug_continue/debug_wait 等待命中。");
    }
    // attach 会话退出（顺手项①，对齐 debug_state）：退出码不可得
    if (active.IsAttach && active.Buffer.CurrentState == DotNetDebugger.Engine.Models.DebugSessionState.Exited)
        sb.AppendLine().Append("（attach 会话：退出码不可得——ICorDebug 不提供，目标独立于调试器退出）");
    foreach (var row in rows)
    {
        sb.AppendLine();
        sb.Append($"[{row.Utc.ToLocalTime():HH:mm:ss.fff}] {row.Tag,-6} {row.Text}");
    }
    return sb.ToString();
}
```
> 复用点：头部/提示文案与 `DebugOutputTool.AppendTargetOutput`/`DebugSessionTool` 对齐（`[HH:mm:ss.fff]` 展示形态沿用）。

- [ ] **Step 2: debug_state 顺手项**

`DebugSessionTool.DebugState`（`:110` 返回体组装处）：在状态行/尾部追加：
```csharp
if (buffer.CurrentState == DotNetDebugger.Engine.Models.DebugSessionState.Exited)
    lines.Add(active.IsAttach
        ? "（attach 会话：退出码不可得——ICorDebug 不提供，目标独立于调试器退出）"
        : active.ExitCode is { } ec ? $"（目标已退出：exitCode={ec}）" : "（目标已退出）");
if (active.IsAttach && buffer.CurrentState == DotNetDebugger.Engine.Models.DebugSessionState.Running)
    lines.Add("attach 会话：已附加成功，进程继续独立运行（无停点不会自行停下）；设断点后 debug_continue/debug_wait 等待命中。");
```
（launch 会话 Exited 时输出缓冲已带 `[进程已退出 exitCode=N]` 标记——debug_output/debug_wait 现成可见，此处补的是 `debug_state` 直达。）

- [ ] **Step 3: 端到端失败测试 → 验证**

`DebugMcpToolsTests` 加：
1. launch `DebugTarget.exe 2 0` → continue → 等退出 → `debug_timeline` 返回含 `log`/`state` 行、时间升序、头部含总量；文本含 `[进程已退出 exitCode=0]`（log 行现成标记）。
2. 设 trace 断点 + 触发异常场景后 `debug_timeline` 含 `trc`/`exc` 行。
3. `debug_state` Exited 显示 `exitCode=0`。
4. 参数错误面：kind 非法返回提示；无会话返回提示。
Run 定向 + 宿主全量。`git commit -m "feat: 新增 debug_timeline 时间线工具 + debug_state 退出码/attach 提示（V3，同步 README/CHANGELOG）"`

---

## 收尾（全部 Task 后）

- [ ] build -c Release 全解决方案 + Session/宿主单测全量 + Client 端到端（`dotnet run -c Release --project src/DotNetDebuggerMcp.Client/...`）。
- [ ] 核对 `src/DotNetDebuggerMcp/TODO.md`：V3 条目状态更新（勾选/移历史）；确认「已评估关闭」区不动。
- [ ] 核对 `docs/planning/specs/README.md` 收录 V3 spec 行。
- [ ] 若在 feature 分支：合并 master（ff）后确认。

## Self-Review（writing-plans 内审）

- **Spec 覆盖**：历史环形 500 含 EngineLog（Task1）；动作入 + TraceHit 双轨 + 不折叠（Task1 AppendHistory 存 TraceHit 原事件、Task2 Summarize trc 摘要、`_traces` 语义未动）；filter/kind/lines（Task2/Task4）；顺手项退出码（Task3）+ attach 提示（Task4）；`ProcessOutputCapture` 全量快照（Task2）。
- **占位符**：无 TBD；`Loc`/`MethodTokenText`/`TimelineRow` 字段名唯一不确定点已注明「以 Models/FrameLocation.cs 为准」并按仓库展示约定给出 fallback 语义。
- **类型一致**：`SnapshotHistory()`/`MaxHistory` Task1 定义 Task2/4 用；`Output.Snapshot()` Task2 定义 Task4 用；`IsAttach`/`ExitCode` Task3 定义 Task4 用；`DebugTimeline.Build` Task2 定义 Task4 用。
- **风险点已标**：同毫秒排序稳定性（TagRank+Seq）；混用 Now/UtcNow 的显示统一本地时刻（DTO 比较按瞬时天然正确）；attach 无 Output（日志为空仍可用）；ExitCode 捕获与 Activate 先后竞态（Exited 在激活后才可能触发）。
