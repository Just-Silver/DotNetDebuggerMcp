# P3 会话层 + MCP 调试工具面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `DotNetDebugger.Session` 库实现会话层（多调试会话管理 + 停点事件缓冲/查询 + agent 轨迹日志），并在宿主 `DotNetDebuggerMcp` 暴露 `debug_*` 系列 MCP 工具（launch/attach/disconnect/breakpoint/continue/step/state/stack/threads/variables/exceptions），让 agent 能经 stdio 完整走通「启动/附加 → 定位断点 → 运行 → 命中 → 观察 → 单步」的调试闭环。

**Architecture:** Session 库依赖 Engine（已有 `DebugSession` 能力）+ Decompiler（用于断点定位辅助与反编译展示）；提供 `DebugSessionManager`（v1 单活动会话）持有活动 `DebugSession`，缓冲其 `DebugEvent` 流为可查询状态（当前是否停、最近停点、线程/栈/变量缓存），并记录 `AgentAction` 轨迹（供 P4 Web 回放）。宿主 MCP 工具经 Session 层路由——控制工具（continue/step 等）**异步返回**（带默认超时参数），停点信息由 `debug_state`/`debug_stack`/`debug_variables` **查询**（用户拍板：异步工具带默认超时 + 断点查询工具）。

**Tech Stack:** .NET 10 / C# / ModelContextProtocol 2.2.0 / DotNetDebugger.Engine（P2）/ DotNetDebugger.Decompiler（反编译辅助）/ xunit.v3

**Spec:** `docs/planning/specs/2026-09-05-overview-design.md`（§4.3 Session 层 + §4.5 MCP 工具面 + §5 线程模型 + §7 P3）+ `decisions.md` D7/D8/D11 + 反编译工具既有模式（宿主 Tools/ 模板）

## Global Constraints

- 命名：`debug_*` 统一前缀（用户拍板）；MCP 工具前缀由注册名决定（`dotnetdebugger_debug_*`）
- Session 库独立（D8）：`src/DotNetDebugger.Session/` → `DotNetDebugger.Session`；依赖 Engine + Decompiler；宿主引 Session
- **异步工具带默认超时实际参数**（用户拍板）：控制工具（launch/attach/continue/step 等）有 `timeoutSeconds` 参数（默认如 30），返回「已启动/已继续」不等停；停点信息经 `debug_state`/`debug_stack`/`debug_variables`/`debug_threads` 查询
- **stdout 纯净纪律不可破**：宿主 stdout 只走 MCP 协议；调试工具日志走 stderr；并发回归护栏延续（MCP 会话并发下不得撕帧——调试会话管理须线程安全）
- MCP 工具参数全带默认值（空串等，AGENTS 铁律）；`[Description]` 中文、面向 agent、注明默认值；工具方法 `CancellationToken cancellationToken = default` 在末尾
- Session 库不反引用宿主/Tools；Engine 保持纯底层（Session 层封装为 agent 友好的查询/命令面）
- 依赖：Session 库引 Engine + Decompiler（复用 MetadataNaming 定位方法 token/反编译展示）
- 测试：Session 库单测（管理器状态机/事件缓冲）+ 宿主 MCP 工具集成测试（真实 DebugTarget 进程，串行）+ 既有 425 反编译测试不回归
- 每个 Task 结束提交；提交信息用中文

---

## 目标文件结构

```
src/DotNetDebugger.Session/
  DotNetDebugger.Session.csproj      # 引 Engine + Decompiler
  Models/
    DebugSessionInfo.cs              # 会话摘要（id/状态/目标/最近停点）
    StopContext.cs                   # 停点现场（reason/threadId/topFrame/时间）
    AgentActionLog.cs                # agent 轨迹记录（工具/参数摘要/结果/关联事件）
  DebugSessionManager.cs             # 多会话管理（v1 单活动）：CreateSession/GetActive/Close
  SessionEventBuffer.cs              # 消费 DebugEvent 流 → 当前状态（running/stopped/停点缓存）
src/DotNetDebuggerMcp/
  Tools/Debug/
    DebugSessionTool.cs              # debug_launch/debug_attach/debug_disconnect/debug_state
    DebugControlTool.cs              # debug_continue/debug_step/debug_pause
    DebugBreakpointTool.cs           # debug_breakpoint（set/remove/clear 子命令或独立）
    DebugInspectTool.cs              # debug_stack/debug_threads/debug_variables
    DebugExceptionTool.cs            # debug_exceptions（设/清异常断点）
  Services/DebugSessionService.cs    # 宿主侧会话服务（挂 AppServices 或独立单例）
tests/DotNetDebugger.Session.Tests/  # Session 库单测
tests/DotNetDebuggerMcp.Tests/       # 宿主 MCP 调试工具集成测试（新增）
```

> 注：具体工具方法粒度（launch/attach 独立 or debug_session 子命令）在 Task 3 定稿时按「agent 友好 + 参数全默认」原则展开；每工具带 timeoutSeconds 默认参数。

---

## Task 总览

- **Task 1** Session 库骨架 + DebugSessionManager + SessionEventBuffer（消费 DebugEvent → 可查询状态），单测
- **Task 2** Session 库 Models（会话摘要/停点现场/轨迹日志）+ DebugSessionManager 生命周期测试
- **Task 3** 宿主 MCP 调试工具：debug_launch/attach/disconnect/state（会话管理面），端到端 attach 验证
- **Task 4** debug_breakpoint/debug_continue/debug_step（控制面），端到端断点命中验证
- **Task 5** debug_stack/debug_threads/debug_variables/debug_exceptions（观察面），端到端状态读取验证
- **Task 6** 并发安全 + 回归护栏（stdout 纯净；MCP 并发工具调用测试）
- **Task 7** 文档同步（README 工具表/CHANGELOG/握手简介）+ 全量验收

---

### Task 1: Session 库骨架 + DebugSessionManager + SessionEventBuffer

**Files:**
- Modify: `src/DotNetDebugger.Session/DotNetDebugger.Session.csproj`（引 Engine + Decompiler）
- Delete: `src/DotNetDebugger.Session/Placeholder.cs`
- Create: `src/DotNetDebugger.Session/DebugSessionManager.cs`
- Create: `src/DotNetDebugger.Session/SessionEventBuffer.cs`
- Test: `tests/DotNetDebugger.Session.Tests/DotNetDebugger.Session.Tests.csproj` + `SessionEventBufferTests.cs` + `DebugSessionManagerTests.cs`

**Interfaces:**
- Consumes: `DotNetDebugger.Engine.DebugSession`（P2）+ `DotNetDebugger.Engine.Models.DebugEvent/DebugSessionState/...`
- Produces: `DebugSessionManager`（活动会话管理）+ `SessionEventBuffer`（DebugEvent 流 → 可查询状态）

**关键设计：**
- `SessionEventBuffer`：订阅 `session.Events`（IAsyncEnumerable），后台消费维护线程安全快照：
  - `SessionState CurrentState`（None/Launching/Running/Stopped/Exited/Detached）
  - `StopContext? LastStop`（最近停点：BreakpointHit/StepCompleted/ExceptionHit 的 reason/threadId/topFrame/时间）
  - `int StoppedThreadId`（-1 = 未停）
  - 事件订阅在 Session 创建后立即开始，保证不错过停点
- `DebugSessionManager`：v1 单活动会话；`LaunchAsync/AttachAsync` 产出 `ActiveSession`（含 buffer）；`CloseAsync` 释放；`Active`/`State` 查询线程安全
- 测试：SessionEventBuffer 用假事件源验证状态推进；DebugSessionManager 用真实 DebugTarget attach 验证（复用 P2 设施）

- [ ] **Step 1: csproj 引依赖**

`src/DotNetDebugger.Session/DotNetDebugger.Session.csproj` 加 ProjectReference：
```xml
  <ItemGroup>
    <ProjectReference Include="..\DotNetDebugger.Engine\DotNetDebugger.Engine.csproj" />
    <ProjectReference Include="..\DotNetDebugger.Decompiler\DotNetDebugger.Decompiler.csproj" />
  </ItemGroup>
```
删除 Placeholder.cs。

- [ ] **Step 2: SessionEventBuffer**

`src/DotNetDebugger.Session/SessionEventBuffer.cs`:
```csharp
using DotNetDebugger.Engine;
using DotNetDebugger.Engine.Models;

namespace DotNetDebugger.Session;

/// <summary>停点现场：进程停在何处/为何停（供 debug_state/debug_stack 等查询）。</summary>
public sealed record StopContext(
    DateTimeOffset UtcTimestamp,
    DebugEventKind Kind,          // BreakpointHit/StepCompleted/ExceptionHit
    int ThreadId,
    FrameLocation? TopFrame,
    string? Reason,               // 断点 id / step reason / 异常类型
    int? BreakpointId = null);

/// <summary>
/// 消费 DebugSession 的 DebugEvent 流，维护线程安全的最新状态快照（供查询工具读取，不等停）。
/// 后台任务从 session.Events 读取，更新 CurrentState/LastStop/StoppedThreadId。
/// </summary>
public sealed class SessionEventBuffer : IAsyncDisposable
{
    private readonly object _gate = new();
    private DebugSessionState _state = DebugSessionState.None;
    private StopContext? _lastStop;
    private CancellationTokenSource _cts = new();
    private Task? _consumer;

    public DebugSessionState CurrentState { get { lock (_gate) return _state; } }
    public StopContext? LastStop { get { lock (_gate) return _lastStop; } }
    public int StoppedThreadId => LastStop?.ThreadId ?? -1;

    /// <summary>开始消费事件流（Session 创建后立即调用，避免错过停点）。</summary>
    public void Start(DebugSession session)
    {
        _cts = new CancellationTokenSource();
        _consumer = Task.Run(async () =>
        {
            await foreach (var e in session.Events.WithCancellation(_cts.Token))
            {
                OnEvent(e);
            }
        });
    }

    private void OnEvent(DebugEvent e)
    {
        switch (e.Kind)
        {
            case DebugEventKind.SessionStateChanged:
                if (e.Payload is SessionStateChangedPayload sp)
                    lock (_gate) _state = sp.State;
                break;
            case DebugEventKind.BreakpointHit:
                if (e.Payload is BreakpointHitPayload bp)
                    lock (_gate) { _state = DebugSessionState.Stopped; _lastStop = new StopContext(e.UtcTimestamp, e.Kind, bp.ThreadId, bp.TopFrame, $"breakpoint {bp.BreakpointId}", bp.BreakpointId); }
                break;
            case DebugEventKind.StepCompleted:
                if (e.Payload is StepCompletedPayload sc)
                    lock (_gate) { _state = DebugSessionState.Stopped; _lastStop = new StopContext(e.UtcTimestamp, e.Kind, sc.ThreadId, sc.TopFrame, sc.Reason); }
                break;
            case DebugEventKind.ExceptionHit:
                if (e.Payload is ExceptionHitPayload ex)
                    lock (_gate) { _state = DebugSessionState.Stopped; _lastStop = new StopContext(e.UtcTimestamp, e.Kind, ex.ThreadId, ex.TopFrame, $"exception {ex.ExceptionType}"); }
                break;
        }
    }

    public async ValueTask DisposeAsync()
    {
        _cts.Cancel();
        if (_consumer is not null) { try { await _consumer; } catch { } }
        _cts.Dispose();
    }
}
```

- [ ] **Step 3: DebugSessionManager**

`src/DotNetDebugger.Session/DebugSessionManager.cs`:
```csharp
using DotNetDebugger.Engine;
using DotNetDebugger.Engine.Models;

namespace DotNetDebugger.Session;

/// <summary>
/// 调试会话管理器：v1 单活动会话。Launch/Attach 建立会话并立即启动事件缓冲；
/// 工具经 Active 访问当前会话与状态（查询不等停，控制另发）。
/// </summary>
public sealed class DebugSessionManager : IAsyncDisposable
{
    private readonly object _gate = new();
    private ActiveDebugSession? _active;

    /// <summary>当前活动会话（无则 null）。</summary>
    public ActiveDebugSession? Active { get { lock (_gate) return _active; } }

    /// <summary>启动新进程并附加（async，带超时参数由工具传）。</summary>
    public async Task<ActiveDebugSession> LaunchAsync(string commandLine, int timeoutSeconds = 30, CancellationToken ct = default)
    {
        var session = await DebugSession.LaunchAsync(commandLine, timeoutSeconds * 1000, null, ct).ConfigureAwait(false);
        return Activate(session);
    }

    /// <summary>附加到已运行进程。</summary>
    public async Task<ActiveDebugSession> AttachAsync(int processId, CancellationToken ct = default)
    {
        var session = await DebugSession.AttachAsync(processId, ct).ConfigureAwait(false);
        return Activate(session);
    }

    private ActiveDebugSession Activate(DebugSession session)
    {
        var buffer = new SessionEventBuffer();
        buffer.Start(session);
        var active = new ActiveDebugSession(session, buffer);
        lock (_gate)
        {
            _active?.Dispose();
            _active = active;
        }
        return active;
    }

    /// <summary>断开并关闭活动会话。</summary>
    public async Task CloseAsync(CancellationToken ct = default)
    {
        ActiveDebugSession? toClose;
        lock (_gate) { toClose = _active; _active = null; }
        if (toClose is not null)
        {
            try { await toClose.Session.DisconnectAsync(ct); } catch { }
            await toClose.DisposeAsync();
        }
    }

    public async ValueTask DisposeAsync() => await CloseAsync();
}

/// <summary>活动会话 = Engine DebugSession + 事件缓冲。</summary>
public sealed class ActiveDebugSession : IAsyncDisposable
{
    public DebugSession Session { get; }
    public SessionEventBuffer Buffer { get; }
    internal ActiveDebugSession(DebugSession session, SessionEventBuffer buffer) { Session = session; Buffer = buffer; }

    public async ValueTask DisposeAsync()
    {
        await Buffer.DisposeAsync();
        await Session.DisposeAsync();
    }
}
```

> 注意：DebugSession.LaunchAsync 第二参数是 timeoutMs（int），此处 `timeoutSeconds * 1000` 换算。ActiveDebugSession 的 Dispose 在 CloseAsync 已调——防重复用标志（实现时加 `_disposed`）。

- [ ] **Step 4: Session 测试项目**

`tests/DotNetDebugger.Session.Tests/DotNetDebugger.Session.Tests.csproj`:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>DotNetDebugger.Session.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.9.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="4.0.0"><PrivateAssets>all</PrivateAssets><IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets></PackageReference>
    <PackageReference Include="xunit.v3" Version="4.0.0" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\DotNetDebugger.Session\DotNetDebugger.Session.csproj" />
    <ProjectReference Include="..\..\src\DotNetDebugger.Engine\DotNetDebugger.Engine.csproj" />
  </ItemGroup>
</Project>
```
加 `AssemblyInfo.cs`：`[assembly: Xunit.v3.Parallelization(Mode = Xunit.Sdk.ParallelMode.None)]`（调试测试串行）。加入 slnx。

- [ ] **Step 5: 测试**

`SessionEventBufferTests.cs`（假 DebugSession 事件源太复杂——用真实 DebugTarget attach 验证状态推进，复用 P2 的 DebugTargetProcess 设施但 Session.Tests 需自带路径解析）：
```csharp
[Fact]
public async Task Launch_ThenState_BecomesRunning() { /* DebugSessionManager.LaunchAsync(DebugTarget delay) → Active.State 最终 Running */ }
[Fact]
public async Task Attach_ThenClose_StateDetached() { /* attach → CloseAsync → State Detached */ }
```
（精确断言按实现时 Engine 实际事件行为定；核心验证 buffer 随事件推进、Close 干净释放。）

- [ ] **Step 6: 提交**

```bash
git add -A && git commit -m "feat: Session 库——DebugSessionManager（单活动会话）+ SessionEventBuffer（DebugEvent 流→可查询状态）+ 测试"
```

---

### Task 2: Session Models + AgentAction 轨迹日志

**Files:**
- Create: `src/DotNetDebugger.Session/Models/DebugSessionInfo.cs`
- Create: `src/DotNetDebugger.Session/AgentActionLog.cs`
- Modify: `src/DotNetDebugger.Session/DebugSessionManager.cs`（记录轨迹）
- Test: `tests/DotNetDebugger.Session.Tests/AgentActionLogTests.cs`

**Interfaces:**
- Consumes: Task 1
- Produces: 会话摘要模型（供 debug_state 返回）+ `AgentActionLog`（每步 agent 指令轨迹，P4 回放源）

**关键设计（spec §4.3 轨迹）：**
- `DebugSessionInfo`：会话 id、目标描述、当前状态、最近停点、活动断点数——debug_state 返回的骨架
- `AgentActionLog`：追加只读环形日志（内存，v1 上限如 1000 条）；每条含序号/时间/工具名/参数摘要/结果摘要
- Session 库的 MCP 工具方法内部调 `LogAction(tool, args, result)` 记录——但 Session 库不应感知 MCP。设计：DebugSessionManager 暴露 `LogAction(string tool, string argsSummary, string resultSummary)`，宿主工具调用它

- [ ] **Step 1: DebugSessionInfo**

```csharp
namespace DotNetDebugger.Session;

/// <summary>调试会话摘要（供 debug_state 返回与轨迹关联）。</summary>
public sealed record DebugSessionInfo(
    string SessionId,
    string TargetDescription,
    DebugSessionState State,
    DateTimeOffset StartedAt,
    StopContext? LastStop,
    int BreakpointCount);
```

- [ ] **Step 2: AgentActionLog**

```csharp
namespace DotNetDebugger.Session;

/// <summary>一条 agent 调试动作记录。</summary>
public sealed record AgentAction(long Sequence, DateTimeOffset UtcTimestamp, string Tool, string ArgsSummary, string ResultSummary);

/// <summary>agent 轨迹环形日志（v1 内存上限 MaxEntries，P4 Web 回放源）。线程安全。</summary>
public sealed class AgentActionLog
{
    public const int MaxEntries = 1000;
    private readonly object _gate = new();
    private readonly Queue<AgentAction> _entries = new();
    private long _seq;

    public void Log(string tool, string argsSummary, string resultSummary)
    {
        lock (_gate)
        {
            _entries.Enqueue(new AgentAction(Interlocked.Increment(ref _seq), DateTimeOffset.UtcNow, tool, argsSummary, resultSummary));
            while (_entries.Count > MaxEntries) _entries.Dequeue();
        }
    }

    public IReadOnlyList<AgentAction> Snapshot()
    {
        lock (_gate) return _entries.ToArray();
    }
}
```

- [ ] **Step 3: Manager 暴露轨迹**

`DebugSessionManager` 加 `public AgentActionLog Actions { get; } = new();`；ActiveDebugSession 也持有（或共享）。宿主工具记录时经 manager.Actions.Log。

- [ ] **Step 4: 测试 + 提交**

AgentActionLogTests：追加/环形上限/快照线程安全。提交 `feat: Session 库 Models + AgentActionLog 轨迹日志`。

---

### Task 3: 宿主 MCP 调试工具（会话管理面：launch/attach/disconnect/state）

**Files:**
- Modify: `src/DotNetDebuggerMcp/DotNetDebuggerMcp.csproj`（ProjectReference Session）
- Create: `src/DotNetDebuggerMcp/Tools/Debug/DebugSessionTool.cs`
- Create: `src/DotNetDebuggerMcp/Services/DebugSessionService.cs`（挂 AppServices 或独立单例持 DebugSessionManager）
- Test: `tests/DotNetDebuggerMcp.Tests/DebugToolsSessionTests.cs`

**Interfaces:**
- Consumes: Task 1-2 Session 库
- Produces: `debug_launch`/`debug_attach`/`debug_disconnect`/`debug_state` 四个 MCP 工具；宿主侧共享 DebugSessionManager

**关键设计（用户拍板：异步带默认超时 + 查询工具）：**
- `debug_launch(commandLine, timeoutSeconds=30)`：async 启动并附加，返回「已启动，会话 id，目标」+ 当前状态；**不等停**
- `debug_attach(processId)`：附加已运行进程
- `debug_disconnect()`：断开活动会话
- `debug_state()`：返回活动会话摘要（状态/最近停点/断点数/目标）——**查询工具，立即返回**
- 共享 manager：宿主 `DebugSessionService`（静态单例持 DebugSessionManager），工具经它路由
- 错误返回中文提示；无活动会话时 debug_state 返回「无活动调试会话」

- [ ] **Step 1: csproj + 服务**

宿主 csproj 加 `ProjectReference Session`。创建 `Services/DebugSessionService.cs`:
```csharp
namespace DotNetDebuggerMcp.Services;

/// <summary>宿主侧共享调试会话服务（Session 库 DebugSessionManager 单例）。</summary>
internal static class DebugSessionService
{
    public static DotNetDebugger.Session.DebugSessionManager Manager { get; } = new();
}
```

- [ ] **Step 2: DebugSessionTool**

`Tools/Debug/DebugSessionTool.cs` 提供 `debug_launch`/`debug_attach`/`debug_disconnect`/`debug_state` 四个 `[McpServerTool]` 方法。方法签名示例：
```csharp
[McpServerToolType]
public static class DebugSessionTool
{
    [McpServerTool]
    [Description("启动并附加一个 .NET 进程进行调试（异步返回，不等停点）。返回会话 id 与初始状态；命中断点后用 debug_state/debug_stack 查询。" + ...)]
    public static async Task<string> DebugLaunch(
        [Description("目标可执行文件路径（可带参数，如 DebugTarget.exe 3 8），相对当前工作目录（必填）。")] string commandLine = "",
        [Description("本次启动等待秒数上限，默认 30。")] int timeoutSeconds = 30,
        CancellationToken cancellationToken = default)
    { ... }

    [McpServerTool] DebugAttach(int processId, ...)
    [McpServerTool] DebugDisconnect(...)
    [McpServerTool] DebugState(...)   // 查询：返回活动会话摘要（状态/最近停点/断点数）
}
```
每个方法内部：调 DebugSessionService.Manager → 记录 AgentAction → 返回中文结果文本。
`debug_launch` 需先 Validate 参数（commandLine 非空 + 目标存在）——复用 ArgumentValidators 或新增（Engine 场景无 assembly 校验，另写）。

- [ ] **Step 3: 端到端测试**

`DebugToolsSessionTests.cs`：真实 DebugTarget attach → debug_launch 后 debug_state 显示 Running → 等目标 delay 结束退出。复用 P2 DebugTargetProcess 设施（Engine.Tests 的——宿主测试需自带或共享；实现时在宿主测试建 DebugTarget 启动辅助）。

- [ ] **Step 4: 提交**

`feat: 宿主 MCP debug_launch/attach/disconnect/state 工具（Session 管理器路由）`

---

### Task 4: debug_breakpoint/continue/step（控制面）

**Files:**
- Create: `src/DotNetDebuggerMcp/Tools/Debug/DebugControlTool.cs`（continue/step）
- Create: `src/DotNetDebuggerMcp/Tools/Debug/DebugBreakpointTool.cs`
- Test: `tests/DotNetDebuggerMcp.Tests/DebugToolsControlTests.cs`

**Interfaces:**
- Consumes: Task 3 会话服务
- Produces: `debug_breakpoint`（set/remove/clear）、`debug_continue`、`debug_step` 工具

**关键设计：**
- `debug_continue(timeoutSeconds=30)`：继续执行（async 返回「已继续」不等停）；**用户拍板：异步带默认超时**
- `debug_step(stepType=over)`：单步（into/over/out）
- `debug_breakpoint(...)`：下断点需定位——agent 提供 `moduleName`+`methodToken`+`ilOffset`（token 可从反编译 signature 拿），或提供 `typeName`+`memberName` 经 Session 用 Decompiler MetadataNaming/MemberResolver 解析 token。**决策：v1 提供两种入口**——按 token 直接设（精确）或按类型+成员名解析（方便）。实现时按 Session 库引 Decompiler 的能力做解析辅助。
- 断点命中后进程停：agent 经 debug_state 发现 Stopped，再 debug_stack/debug_variables 观察

- [ ] **Step 1-3**: 实现三工具 + 端到端（attach → breakpoint(token) → continue → 轮询 debug_state 直到 Stopped → step → continue）
- [ ] **Step 4**: 提交

---

### Task 5: debug_stack/threads/variables/exceptions（观察面）

**Files:**
- Create: `src/DotNetDebuggerMcp/Tools/Debug/DebugInspectTool.cs`
- Create: `src/DotNetDebuggerMcp/Tools/Debug/DebugExceptionTool.cs`
- Test: `tests/DotNetDebuggerMcp.Tests/DebugToolsInspectTests.cs`

**Interfaces:**
- Consumes: Task 4 停点能力
- Produces: `debug_stack`/`debug_threads`/`debug_variables`/`debug_exceptions` 工具

**关键设计：**
- `debug_stack(threadId=-1)`：进程停时读调用栈（缺省用最近停点线程）；未停返回提示
- `debug_threads()`：线程列表
- `debug_variables(threadId=-1)`：局部变量/参数（v1 标量）
- `debug_exceptions(typeName="")`：设 first-chance 异常断点（空=全部）
- 全部查询工具立即返回（进程停时数据有效；未停提示「进程运行中，先 debug_pause/等断点」）

- [ ] **Step 1-3**: 实现 + 端到端（attach → breakpoint → continue → 命中 → debug_stack/debug_variables 输出栈与变量）
- [ ] **Step 4**: 提交

---

### Task 6: 并发安全 + 回归护栏

**Files:**
- Modify: `tests/DotNetDebuggerMcp.Tests/`（新增调试工具并发测试）
- Test: 复用 `McpSessionConcurrencyTests` 模式——调试工具并发调用不撕帧

**Interfaces:**
- Consumes: Task 3-5
- Produces: 验证 MCP 并发下调式工具安全（Session 管理器线程安全 + stdout 纯净）

**关键：** DebugSessionManager/ActiveDebugSession/SessionEventBuffer 均线程安全（lock/volatile）；并发工具调用经各自 await 不共享可变状态（每工具调 manager，manager 内部锁）。用 `McpSessionConcurrencyTests` 同款子进程并发护栏验证。

- [ ] **Step 1-3**: 并发测试（多路 debug_state 并发 + 与反编译工具混打）→ 提交

---

### Task 7: 文档同步 + 全量验收

**Files:**
- Modify: `README.md`（工具表加 debug_*）、`AppText.HandshakeFeatureIntro`（工具一览加 debug_*）、`CHANGELOG.md [Unreleased]`、`src/DotNetDebuggerMcp/.mcp/server.json` 若工具面影响描述
- Modify: `docs/planning/`（P3 完成标记）

**Interfaces:**
- Consumes: Task 1-6
- Produces: 文档与包能力一致；全量测试绿

- [ ] **Step 1**: README 工具表 + 参数表 + 示例加 debug_* 工具（含「异步带超时/查询」约定）
- [ ] **Step 2**: HandshakeFeatureIntro 工具一览加 debug_*（AGENTS 纪律）
- [ ] **Step 3**: CHANGELOG [Unreleased] 记 P3（MCP 调试工具面）
- [ ] **Step 4**: 全量验收——Engine 6 + Session 测试 + 宿主 425+调试工具测试 + Client 端到端 + `-dbg` CLI 仍可用
- [ ] **Step 5**: 提交 + 规划文档 P3 完成标记

---

## Self-Review 记录（P3 计划）

- **Spec 覆盖**：§4.3 Session 层（Task 1-2）、§4.5 工具面（Task 3-5）、§5 线程模型（Task 1 buffer/manager 线程安全 + Task 6 护栏）、§7 P3（全部）。Gap：DocumentService（断点定位反编译展示）未独立——v1 经 Decompiler 直接解析 token（Task 4），完整 DocumentService 列 P4（Web 需要文档模型时）。session 工具命名按用户拍板 debug_*。
- **占位符扫描**：Task 3-5 方法体给出签名骨架但非完整实现——属「宿主工具按既有反编译工具模式实现」的合理展开点（模板已在 Global Constraints 引用）；关键决策（异步带超时/查询/双入口断点定位）已明确。无 TBD。
- **类型一致性**：DebugSessionManager/ActiveDebugSession/SessionEventBuffer/StopContext 在 Task 1-2 定义、Task 3-5 沿用；DebugSession（Engine）API 与 P2 一致。
- **依赖**：Session 库引 Engine+Decompiler；宿主引 Session。不引 sharpdbg/dnSpy。

---

